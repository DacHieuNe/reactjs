import React from 'react';
import PropTypes from 'prop-types';

const Error = props => {
    return (
        <div>
            <h3>Page Error</h3>
        </div>
    );
};

Error.propTypes = {
    
};

export default Error;